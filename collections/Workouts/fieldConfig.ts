import { commonFieldConfig } from '@/collections/coomonFieldConfig';

export const workoutFieldConfig = {
  exercise: commonFieldConfig.exercise,
  userId: 'userId',
  date: commonFieldConfig.date,
  weight: 'weight',
  workWeight: 'workWeight',
  repetitions: 'repetitions',
};
